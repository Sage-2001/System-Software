<!-- 1. Create the following types of a files using (i) shell command (ii) system call
a. soft link (symlink system call)
b. hard link (link system call)
c. FIFO (mkfifo Library Function or mknod system call) -->

<!-- 1 a. 1
SoftLink using Shell command :ln -s 1 SoftLink1
You can check it out using :ls -li 
-->

<!-- 2 a. 1
HardLink using Shell command :ln -s 1 SoftLink1
You can check it out using :ls -li 
-->

<!-- 3 a. 1
Create the pipe using :mkfifo pipename
Suppose you want to read from your pipe just say: cat pipename
OPen the other terminal echo "some message" > pipename 
This will got printed into the actual bash which is waiting for something to be read
-->


